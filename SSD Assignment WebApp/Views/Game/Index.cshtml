@model SSD_Assignment_WebApp.Models.Game

@foreach (List<GridSpace> row in Model.Map)
{
    @foreach (GridSpace g in row)
    {
        @if (g.Building == 'R')
        {
            <img src="~/img/residential.jpg" width="50" />
        }

        @if (g.Building == 'I')
        {
            <img src="~/img/Industry.jpg" width="50" />
        }

        @if (g.Building == 'C')
        {
            <img src="~/img/commercial.jpg" width="50" />
        }

        @if (g.Building == 'O')
        {
            <img src="~/img/park.jpg" width="50" />
        }

        @if (g.Building == '*')
        {
            <img src="~/img/road.jpg" width="50" />
        }

        @if (g.Building == 'N')
        {
            <img src="~/img/empty.jpg" width="50"/>
        }

    }
    <br />

}
